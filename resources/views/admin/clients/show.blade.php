@extends('layouts/layoutMaster') @section('title',$client->name) @section('content') @include('admin.partials.flash')<div class="card mb-6"><div class="card-body"><div class="d-flex justify-content-between"><div><h4>{{ $client->name }}</h4><p class="mb-0">{{ $client->legal_name }}</p></div><div><a class="btn btn-outline-primary" href="{{ route('clients.edit',$client) }}">Editar</a><form class="d-inline" method="POST" action="{{ route('clients.toggle',$client) }}">@csrf @method('PATCH')<button class="btn btn-outline-secondary" onclick="return confirm('¿Cambiar estado del cliente?')">{{ $client->active?'Desactivar':'Activar' }}</button></form></div></div></div></div><div class="card"><div class="card-header"><h5>Instalaciones</h5></div><div class="list-group list-group-flush">@forelse($client->instances as $i)<a class="list-group-item list-group-item-action" href="{{ route('instances.show',$i) }}">{{ $i->name }} · {{ $i->db_name }}</a>@empty<div class="list-group-item">Sin instalaciones.</div>@endforelse</div></div>@endsection
