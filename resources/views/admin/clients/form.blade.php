@extends('layouts/layoutMaster') @section('title',$client->exists?'Editar cliente':'Nuevo cliente') @section('content')<div class="card"><div class="card-header"><h4>{{ $client->exists?'Editar cliente':'Nuevo cliente' }}</h4></div><div class="card-body">@include('admin.partials.flash')<form method="POST" action="{{ $client->exists?route('clients.update',$client):route('clients.store') }}">@csrf @if($client->exists)@method('PUT')@endif<div class="row g-4">@foreach(['name'=>'Nombre *','legal_name'=>'Razón social','rfc'=>'RFC','email'=>'Email','phone'=>'Teléfono'] as $field=>$label)<div class="col-md-6"><label class="form-label">{{ $label }}</label><input class="form-control" name="{{ $field }}" value="{{ old($field,$client->$field) }}" {{ $field==='name'?'required':'' }}></div>@endforeach<div class="col-12"><label class="form-label">Notas</label><textarea class="form-control" name="notes">{{ old('notes',$client->notes) }}</textarea></div><div class="col-12"><button class="btn btn-primary">Guardar</button></div></div></form></div></div>@endsection
