@extends('layouts/layoutMaster') @section('title','Registrar instalación existente') @section('content')<div class="card"><div class="card-header"><h4>Registrar instalación existente</h4><p class="mb-0">La conexión usará el usuario MySQL global configurado.</p></div><div class="card-body">@include('admin.partials.flash')<form method="POST" action="{{ route('instances.store') }}">@csrf<div class="row g-4"><div class="col-md-6"><label class="form-label">Cliente</label><select class="form-select" name="client_id" required>@foreach($clients as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select></div>@foreach(['name'=>'Nombre','slug'=>'Slug','folder_name'=>'Carpeta','domain'=>'Dominio','url'=>'URL','db_name'=>'Base de datos'] as $f=>$l)<div class="col-md-6"><label class="form-label">{{ $l }}</label><input class="form-control" name="{{ $f }}" value="{{ old($f) }}" {{ in_array($f,['name','slug','folder_name','db_name'])?'required':'' }}></div>@endforeach<div class="col-12"><button class="btn btn-primary">Registrar</button></div></div></form></div></div>@endsection
