@extends('layouts/layoutMaster') @section('title',$instance->name) @section('content') @include('admin.partials.flash')<div class="card mb-6"><div class="card-body"><div class="d-flex justify-content-between align-items-start"><div><h4>{{ $instance->name }}</h4><span class="badge bg-label-primary">{{ $instance->installation_status->value }}</span> <span class="badge bg-label-{{ $instance->last_connection_status==='CONNECTED'?'success':'danger' }}">{{ $instance->last_connection_status==='CONNECTED'?'Conectada':'Sin conexión' }}</span></div><form method="POST" action="{{ route('instances.test',$instance) }}">@csrf<button class="btn btn-primary">Probar conexión</button></form></div><hr><dl class="row mb-0"><dt class="col-sm-3">UUID</dt><dd class="col-sm-9"><code>{{ $instance->uuid }}</code></dd><dt class="col-sm-3">Cliente</dt><dd class="col-sm-9">{{ $instance->client->name }}</dd><dt class="col-sm-3">Carpeta</dt><dd class="col-sm-9">{{ $instance->absolute_path }}</dd><dt class="col-sm-3">Base</dt><dd class="col-sm-9"><code>{{ $instance->db_name }}</code></dd></dl>@if($instance->installation_status->value==='READY_FOR_DEPLOY')<div class="alert alert-info mt-5">Ahora cree manualmente el subdominio en cPanel y apúntelo a:<br><code>{{ $instance->absolute_path }}/public</code></div>@endif</div></div><div class="card"><div class="card-header"><h5>Bitácora de instalación</h5></div><div class="table-responsive"><table class="table"><thead><tr><th>Fecha</th><th>Paso</th><th>Estado</th><th>Mensaje</th></tr></thead><tbody>@forelse($instance->installationLogs as $l)<tr><td>{{ $l->created_at }}</td><td>{{ $l->step }}</td><td><span class="badge bg-label-{{ $l->status==='SUCCESS'?'success':($l->status==='FAILED'?'danger':'info') }}">{{ $l->status }}</span></td><td>{{ $l->message }}</td></tr>@empty<tr><td colspan="4">Sin eventos.</td></tr>@endforelse</tbody></table></div></div>@endsection
